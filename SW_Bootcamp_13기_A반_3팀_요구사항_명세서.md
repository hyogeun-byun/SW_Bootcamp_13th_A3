# SW Bootcamp 13기 A반 3팀 — 요구사항 명세서

> **프로젝트명**: 이구동성 텔레파시 (On-Device AI Party Game)
> **작성일**: 2026-06-25
> **최종 수정일**: 2026-06-30
> **팀명**: A반 3팀
> **GitHub**: https://github.com/hyogeun-byun/OnDeviceAI

---

## 프로젝트 개요

제시어를 보고 **똑같은 동작**을 취하세요. 평상시에는 서로의 실제 카메라 화면을 볼 수 없고, 오직 **텔레파시 게이지**와 AI MC의 코칭만으로 동작을 맞춥니다. 단, 한 제시어에서 오래 막히면 카메라 힌트가 잠깐 공개되어 다시 동작을 맞출 수 있습니다.

본 프로젝트는 라즈베리파이 기반 카메라 워커, FastAPI 서버, 브라우저 참가자/관객 화면으로 구성된 **온디바이스 AI 파티 게임**입니다. 각 카메라 워커는 로컬에서 MediaPipe Pose Lite로 포즈 랜드마크를 추정하고, 서버는 여러 참가자의 얼굴 방향과 상체 bone 벡터 유사도 및 활동성을 계산해 실시간 텔레파시 점수를 제공합니다. 게임은 60초 스프린트 방식으로 진행되며, 참가자들이 같은 동작을 취해 게이지 90점 이상을 달성하면 해당 제시어를 클리어하고 다음 제시어로 넘어갑니다. 로컬 LLM/정적 폴백 기반 진행자는 한국어 진행 멘트를 제공하며, 네트워크가 제한된 환경에서도 게임 핵심 기능이 동작하도록 폴백 경로를 제공합니다.

---

## 명세 작성 기준

- 요구사항 ID는 기능/비기능을 미리 분류하지 않고 `R-01`, `R-02` 형식의 중립 번호를 사용합니다.
- 검증 기준은 평가자 또는 평가 Agent가 완료 여부를 판단할 수 있도록 화면, 로그, API 응답, 설정값, 파일 산출물 기준으로 작성합니다.
- 시연 영상으로 확인 가능한 항목은 `MM:SS` 형식의 타임스탬프를 적고, 영상으로 확인하기 어려운 항목은 `N/A (사유)`로 표기합니다.

---

## 요구사항 목록

| 요구사항 ID | 요구사항 내용 | 검증 기준 (완료 조건) | 시연 타임스탬프 |
|---|---|---|---|
| R-01 | 여러 대의 카메라 워커가 서버에 WebSocket으로 각각 독립 연결된다 | 카메라 2대 이상이 동시에 접속·스트리밍되며, 한 대 종료 시 다른 카메라 스트림과 게임 상태가 계속 유지된다 | N/A (하드웨어 구성 검증) |
| R-02 | 카메라 워커가 캡처한 영상을 JPEG으로 인코딩해 실시간으로 서버에 전송한다 | 서버 대시보드(`/`)에서 각 카메라 MJPEG 스트림이 끊김 없이 표시된다 | 00:10 |
| R-03 | 카메라 워커가 MediaPipe Pose Lite로 초당 10회 포즈 랜드마크 33개를 추정한다 | 워커 로그 또는 대시보드 지표에서 `pose_fps`가 9fps 이상이고, 서버가 카메라별 랜드마크 33개를 수신한다 | N/A (로그/지표 검증) |
| R-04 | 포즈 추정과 영상 전송이 별도 스레드에서 독립 실행되어 서로를 차단하지 않는다 | 포즈 추론 중에도 `frame_fps`가 설정 FPS에 근접하게 유지되고, 프레임 전송 실패가 누적되지 않는다 | N/A (로그/지표 검증) |
| R-05 | 서버 대시보드(`/`)에서 카메라별 연결 상태·사람 감지 여부·키포인트 수·성능 지표를 실시간으로 확인할 수 있다 | 대시보드에 카메라 상태(Online/Waiting), 포즈 감지 상태, 키포인트 수, FPS, 업로드 KB/s가 주기적으로 갱신된다 | 00:20 |
| R-06 | 대시보드에서 카메라 영상 위에 스켈레톤(키포인트 + 연결선)이 오버레이된다 | 브라우저 Canvas에 감지된 키포인트와 관절 연결선이 실시간으로 그려지고, 사람이 사라지면 오버레이가 지워진다 | 00:30 |
| R-07 | 시작 화면에서 팀명을 입력한 뒤 T자 포즈를 유지하면 AI MC 인트로가 자동 시작된다 | 팀명이 `/api/game/stage`에 저장되고, T자 포즈 유지 진행률이 100%가 되면 `idle`에서 `intro` 단계로 전환된다. 버튼 수동 시작도 동일 흐름으로 동작한다 | 00:40 |
| R-08 | 인트로 후 6개 카테고리(상황 / 운동 / 감정 / 영화 혹은 애니메이션 / 인물 연령대별 2종) 중 하나를 몸동작 또는 버튼으로 선택할 수 있다 | 카테고리 선택 화면에서 왼손 들기/오른손 들기로 항목이 이동하고, T자 포즈 유지 또는 버튼 클릭으로 선택 카테고리가 확정된다 | 00:45 |
| R-09 | 선택한 카테고리의 제시어 풀이 로드되고, 첫 제시어 고정 + 나머지 무작위 순서로 60초 스프린트 동안 순차 제시된다 | `prompt_source`가 `category_random_in_theme`으로 설정되고, 선택 카테고리의 후보 제시어가 로드된다. 게이지 90점 이상이면 다음 제시어로 넘어가며, 풀이 부족하면 순환한다 | 01:00 |
| R-10 | 팀명을 입력하면 게임 종료 후 클리어 개수와 소요 시간이 SQLite 리더보드에 누적 저장되고 순위가 표시된다 | 2팀 이상 플레이 후 리더보드에 팀명·클리어 수·등급·순위가 표시되고, 점수 내림차순 및 동점 시 더 빠른 `total_time` 순으로 정렬된다 | 01:10 |
| R-11 | 게임은 인트로 → 카테고리 선택 → 카메라 테스트 → 카운트다운(3초) → 제시어 공개(2.6초) → 동작 구간으로 자동 진행된다 | 상태머신이 `intro/category/camtest/confirm/countdown/reveal/playing/result/giveup/timeup/finished` 단계를 자동 전환하고, 동작 구간은 전체 60초 스프린트와 제시어별 20초 제한을 적용한다. 제시어 클리어 후 결과 화면은 약 3.0초 동안 표시된 뒤 다음 제시어 공개 또는 종료 화면으로 전환된다 | 01:20 |
| R-12 | 동작 구간 중 참가자들의 얼굴 방향과 상체 6개 bone 벡터 유사도를 실시간으로 계산해 텔레파시 게이지(0~100)로 표시한다 | 플레이어 2명 이상이 같은 동작을 취하면 게이지가 올라가고, 동작이 달라지면 게이지가 내려간다 | 01:30 |
| R-13 | 점수 계산에 활동성 게이트를 적용해 모두 가만히 서 있을 때 유사도가 높아도 낮은 점수가 나온다 | 모두 자연스럽게 서 있는 경우 점수가 50 이하로 유지되고, 팔·다리 움직임이 큰 동작에서는 점수가 더 높게 반영된다 | N/A (동작 조건 반복 측정) |
| R-14 | 참가자 화면(`/game`)은 평상시 다른 플레이어의 실제 카메라 영상을 숨기고 제시어·게이지·AI MC 코칭·타이머만 표시한다 | `/game`의 기본 동작 화면에는 카메라 스트림이 상시 표시되지 않으며, 카메라 힌트가 활성화될 때만 최신 카메라 스냅샷을 짧은 주기로 갱신해 보여준다. 힌트 종료 시 이미지 `src`를 해제해 카메라 연결과 브라우저 리소스를 정리한다 | 01:50 |
| R-15 | 한 제시어를 10초 동안 클리어하지 못하면 참가자 화면에 카메라 힌트 오버레이가 잠깐 표시된다 | `playing` 상태에서 제시어 경과 시간이 10초 이상이면 `show_hint_cams`가 활성화되고, 약 2.5초 동안 각 카메라 최신 스냅샷이 `cam-hint-overlay`에 표시된 뒤 자동으로 숨겨진다 | 01:55 |
| R-16 | 관객 화면(`/stage`)에는 모든 카메라 실시간 영상, 게이지, 코칭, 타이머, 라운드 점수가 동시에 표시된다 | `/stage` 페이지에서 카메라 수만큼 영상 피드가 보이고, 게이지·타이머·라운드 상태가 게임 상태와 동기화된다 | 02:00 |
| R-17 | 게임 상태(phase, gauge, 제시어, 점수 등)가 WebSocket으로 10Hz 수준으로 브로드캐스트되어 참가자·관객 화면이 동기화된다 | 참가자 화면과 관객 화면의 게이지·단계 표시가 1초 이내 오차로 일치한다 | N/A (기술/동기화 검증) |
| R-18 | AI MC(LLM/정적 폴백)가 게임 인트로·라운드 결과·최종 종료 구간에서 한국어 진행 멘트를 생성한다 | LLM 활성 시 각 구간에서 상황에 맞는 한국어 문장이 화면에 표시되고, 비활성 또는 실패 시 정적 멘트가 표시된다 | 02:10 |
| R-19 | 동작(playing) 구간의 실시간 코칭은 LLM 호출 없이 정적 규칙 기반 멘트로 제공되어 포즈 추론 성능에 영향을 주지 않는다 | playing 구간에서는 `narrator.coach()` 기반 문구만 갱신되고, LLM 기반 결과 멘트·최종 리포트 생성은 결과/종료 구간의 백그라운드 작업으로만 실행된다 | N/A (로그 검증) |
| R-20 | LLM 또는 TTS가 실패할 경우 정적 텍스트 또는 브라우저 Web Speech로 자동 폴백되며 게임이 중단되지 않는다 | `LLM_ENABLED=false` 또는 TTS 실패 상황에서도 게임이 정상 진행되고 화면에 기본 멘트가 표시된다 | N/A (설정/장애 주입 검증) |
| R-21 | edge-tts(`ko-KR-InJoonNeural`)로 합성한 mp3를 브라우저가 재생하고, 2D MC 아바타의 말풍선·입 애니메이션이 동작한다 | 음성 재생 중 아바타 말풍선과 입 애니메이션이 활성화되고, 음성 종료 후 비활성화된다 | 02:20 |
| R-22 | 동일한 음성이 중복 재생되지 않도록 `speech_id`로 관리되며, edge-tts 실패 시 브라우저 Web Speech로 폴백된다 | 같은 `speech_id`의 음성이 두 번 재생되지 않고, edge-tts 응답 실패 시 브라우저 음성으로 한 번만 출력된다 | N/A (기술 검증) |
| R-23 | 게임 중 어느 단계에서든 `↺ 처음부터` 버튼으로 초기화해 게임 시작 화면으로 되돌아갈 수 있다 | playing·result·finished 상태에서 버튼 클릭 후 idle 상태로 전환되고 이전 팀명·라운드 스냅샷 상태가 초기화된다 | 02:35 |
| R-24 | 게임 종료 후 60초 동안 클리어한 제시어 수, 종료 소요 시간, 라운드별 점수, 최종 등급 문구가 표시된다 | 최종 화면에 `total_score`(클리어 수), `final_seconds`, `round_scores`, `final_title`, AI 최종 리포트가 표시된다 | 02:45 |
| R-25 | 라운드 결과 화면에서 실제 순간 사진 위에 스켈레톤을 오버레이하고, 종료 후 베스트/최저 라운드 리포트를 PNG로 저장할 수 있다 | 결과 카드 Canvas에 `/api/game/result-frame/{round}/{player}.jpg` 사진과 포즈 스켈레톤 합성본이 보이고, 저장 버튼 클릭 시 1080×1350 PNG 파일이 다운로드된다 | 02:55 |
| R-26 | 인터넷 미연결 LAN 환경에서도 포즈 추론·게임 진행·로컬 LLM·브라우저 TTS 폴백이 동작한다 | 외부 인터넷을 차단해도 카메라 워커, 서버, 브라우저 게임 1회가 완주되며 edge-tts 실패 시 폴백 음성 또는 텍스트가 사용된다 | N/A (환경 조건 검증) |
| R-27 | 카메라 워커와 서버 설정(서버 IP, 카메라 ID, FPS, 모델 경로, LLM/TTS 옵션 등)을 `.env` 또는 예시 설정 파일로 관리한다 | `.env.example`과 설정 예시 파일을 기준으로 실행에 필요한 값이 문서화되고, 기본 모델 경로가 자동 로드된다 | N/A (설정 파일 검증) |

---

## 증빙 및 검증 메모

- 영상 시연 항목은 최종 시연영상의 해당 타임스탬프에서 화면 동작을 확인합니다.
- 2026-06-30 기준 명세는 5라운드 평균 방식이 아니라 60초 스프린트/90점 클리어/20초 제시어 제한/10초 카메라 힌트 방식의 최신 구현을 기준으로 작성했습니다. 최종 시연영상 재촬영 또는 편집 후 타임스탬프를 실제 영상 기준으로 다시 확인해야 합니다.
- `N/A` 항목은 하드웨어 구성, 로그, 설정 파일, 장애 주입, 네트워크 조건처럼 영상만으로 판정하기 어려운 항목이며 실행 로그·README·설정 파일로 보조 증빙합니다.
- 결과파일에는 서버/카메라 워커 소스, `requirements.txt`, 실행 스크립트, 설정 예시, Git 히스토리 또는 `git log` 결과를 포함해야 합니다.
- `edge-tts`는 외부 Microsoft TTS 서비스를 사용할 수 있으므로 완전 오프라인 검증에서는 브라우저 Web Speech 또는 정적 텍스트 폴백을 성공 기준으로 봅니다.
- R-26 오프라인 LAN 검증은 하나의 공유기를 내부 LAN으로만 사용하고 WAN/인터넷을 차단한 뒤, 서버 LAN IP 기준으로 카메라 워커와 브라우저가 연결되어 60초 스프린트 1회를 종료까지 진행하는 방식으로 보여줍니다. 상세 절차는 `test-results/offline-lan/R-26-offline-lan-guide.md`에 작성했습니다.
